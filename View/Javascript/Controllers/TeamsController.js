'use strict';

var AppControllers = angular.module('AppControllers');
AppControllers.controller('teamsCtrl', ['$http', '$scope', 'socket', '$filter', function($http, $scope, socket){
  // ----- Init -----
  
  // ----- GET / SET Data -----
  
  // ----- Public Méthode -----
  
  // ----- Private Méthode -----
  
  // ----- jQuery -----
  
}]);